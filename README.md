# Wowowo-Vue

**Vue3 核心功能实现解析（基于源码学习）**

> 📚 本项目通过实现 Vue3 的核心响应式模块（如 `reactive`、`effect`、`ref` 等），帮助开发者深入理解其底层原理。

## 运行命令

```bash
# 这样可以生成packages/reactivity的打包文件
# 输出路径在packages/reactivity/dist下
npm run dev reactivity
```
